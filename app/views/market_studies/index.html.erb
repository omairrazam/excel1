



<div class = "narrow-container">
<p id="notice"><%= notice %></p>
  <h2>Listing Market Studies</h2>
  <hr>
  
  <table class="table table-striped">
  <thead>
    <thead>
      <tr>
        <th>Date of Post</th>
        <th> Title</th>
        <th>Content</th>
        <% if current_user.present? and current_user.is_admin? %>
          <th></th>
        <% end %>
      </tr>
    </thead>
  </thead>
  <tbody>
    <% @market_studies.each do |market_study| %>
      <tr>
        <td><%= market_study.created_at.strftime("%B %d, %Y") %></td>
      <td><%= link_to market_study.title , market_study%></td>
      <td><%= market_study.content %></td>
      <% if current_user.present? and current_user.is_admin? %>
        <td><span class="glyphicon glyphicon-scissors" aria-hidden="true"></span> &nbsp;&nbsp;<%= link_to 'Edit', edit_market_study_path(market_study)  %></td>
      <% end %>
    </tr>
      <% end %>
    
  </tbody>

  </table>

  
</div>


</div>

<!--
<div class = "narrow-container">
<p id="notice"><%= notice %></p>

<h1>Listing Market Studies</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Content</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @market_studies.each do |market_study| %>
      <tr>
        <td><%= market_study.title %></td>
        <td><%= market_study.content %></td>
        <td><%= link_to 'Show', market_study %></td>
        <td><%= link_to 'Edit', edit_market_study_path(market_study) %></td>
        <td><%= link_to 'Destroy', market_study, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Market study', new_market_study_path %>
</div>